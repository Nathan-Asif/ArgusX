spring.application.name=argusx-compliance-service
server.port=8081
spring.jackson.property-naming-strategy=SNAKE_CASE

# Supabase / PostgreSQL (optional — in-memory audit log used when unset)
argusx.database.url=${ARGUSX_DATABASE_URL:}
argusx.database.enabled=${ARGUSX_DATABASE_ENABLED:false}
